var make = require('./make_scheme');
var makeReference = require('./make_reference_scheme');
var transform = require('./transform_scheme');

module.exports = {
  make: make,
  makeReference: makeReference,
  transform: transform,
};
